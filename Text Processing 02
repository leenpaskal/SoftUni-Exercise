function hashTag(input){

    let array = input.split(" ").filter(word => word.startsWith("#") && word.length > 1);

    for(let item of array){
        item = item.substring(1);

        let template = /^[A-Za-z]+$/;

        if(template.test(item)){
        console.log(item);
        }
    }

}

hashTag(
    'Nowadays everyone uses # to tag a #special word in #socialMedia'
)
